@model Askrindo.Areas.RefData.Models.OrgStruct.SubDivViewModel

@{
    ViewBag.Title = "SubDivNew";
    Layout = "~/Areas/RefData/Views/Shared/_orgstruct.cshtml";
}

<h3 style="padding-bottom: 0; margin-bottom: 0">Bagian Baru</h3>
@Html.Partial("_Info_Division", Model.Division)

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <table class="edit">
        <tr>
            <td>ID</td>
            <td>
                @Html.EditorFor(model => model.SubDiv.SubDivId)
                @Html.ValidationMessageFor(model => model.SubDiv.SubDivId)
            </td>
        </tr>
        <tr>
            <td>Nama Divisi</td>
            <td>
                @Html.EditorFor(model => model.SubDiv.SubDivName)
                @Html.ValidationMessageFor(model => model.SubDiv.SubDivName)
            </td>
        </tr>
        <tr>
            <td>Nama Divisi</td>
            <td>
                @Html.EditorFor(model => model.SubDiv.IsSupporting)
                @Html.ValidationMessageFor(model => model.SubDiv.IsSupporting)
            </td>
        </tr>
    </table>
    
    <div style="padding-top: 12px">
        <input type="submit" value="Simpan" /> |
        @Html.ActionLink("Batal", "SubDivList", new { divId = @Model.Division.DivisionId }, null)
    </div>
}